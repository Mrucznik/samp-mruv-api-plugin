// built-in include guard removal
// just in case the user has a local dependency with the same file name
#if defined _inc_SAMP_MRUV_API_PLUGIN
	#undef _inc_SAMP_MRUV_API_PLUGIN
#endif
// custom include-guard to ensure we don't duplicate
#if defined _SAMP_MRUV_API_PLUGIN_included
	#endinput
#endif
#define _SAMP_MRUV_API_PLUGIN_included

// Enums

// Items service
native mruv_itemsService_status();
native mruv_itemsService_version();

native mruv_itemTypes_create(&id, const name[], const description[], const Float:weight, const Float:volume, const model);
native mruv_itemTypes_create_async(const callback[], const name[], const description[], const Float:weight, const Float:volume, const model);
native mruv_itemTypes_get(const id);
native mruv_itemTypes_delete();
native mruv_itemTypes_getAll();

native mruv_items_create();
native mruv_items_get();
native mruv_items_delete();
native mruv_items_getAll();

native mruv_containerTypes_create();
native mruv_containerTypes_get();
native mruv_containerTypes_delete();
native mruv_containerTypes_getAll();

native mruv_containers_create();
native mruv_containers_get();
native mruv_containers_delete();
native mruv_containers_getAll();
// Code generated by protoc-gen-pawn. DO NOT EDIT.
// versions:
// 	protoc-gen-pawn v0.1.0
// 	protoc          (unknown)

// ---------- Enums ----------
//Sorting modes for container items.
enum SortingMode
{
    //undefined behaviour
    UNKNOWN = 0,
    //sort by weight descending
    WEIGHT_DESC = 1,
    //sort by weight ascending
    WEIGHT_ASC = 2,
    //sort by volume descending
    VOLUME_DESC = 3,
    //sort by volume ascending
    VOLUME_ASC = 4
};
// ---------- Messages ----------
//Item type data structure
enum ItemType
{
    eId,
    eName[256],
    eDescription[256],
    Float:eBaseWeight,
    Float:eBaseVolume,
    eModelName[256],
    eModelHash
};

//Item type ID.
enum ItemTypeID
{
    eId
};

//Item data structure.
enum Item
{
    eId,
    eItemTypeId,
    Float:eWeight,
    Float:eVolume
};

//Item ID.
enum ItemID
{
    eId
};

//Container type data structure.
enum ContainerType
{
    eId,
    eContainerItemTypeId,
    eMaxNumber,
    Float:eMaxVolume,
    Float:eMaxWeight,
    eValidItemTypes[256]
};

//Container type ID.
enum ContainerTypeID
{
    eId
};

//Container data structure.
enum Container
{
    eId,
    eTypeId,
    eItemId,
    eItemsInside,
    eItems[256][InsideItem]
};

//Container ID.
enum ContainerID
{
    eId
};

//Item inside container data structure.
enum InsideItem
{
    eContainerId,
    eItemId,
    eItem,
    ePosition
};

// ---------- Natives ----------
// ---------- Callbacks ----------
